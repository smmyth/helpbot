# Variáveis de Ambiente para o HelpBot

# Chave secreta para o Flask (importante para sessões, segurança, etc.)
# Gere uma chave segura, ex: python -c 'import secrets; print(secrets.token_hex(16))'
SECRET_KEY=sua_chave_secreta_aqui

# URI de conexão do MongoDB
MONGO_URI=mongodb://mongo:27017/helpbot

# Chave da API da OpenAI (opcional, mas necessária para respostas da IA)
OPENAI_API_KEY=sk-suaChaveDaOpenAIAqui

# URL do Webhook do n8n para integrações (opcional)
N8N_WEBHOOK_URL=https://seu-n8n.exemplo.com/webhook/seu-path

# Ambiente do Flask (development, production, testing)
FLASK_ENV=development

# (Opcional) URI do MongoDB para testes (se diferente)
TEST_MONGO_URI=mongodb://localhost:27017/helpbot_test

# (Opcional) Configurações do modelo OpenAI
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_TEMPERATURE=0.7
# OPENAI_MAX_TOKENS=150